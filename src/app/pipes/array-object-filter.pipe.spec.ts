import { ArrayObjectFilterPipe } from './array-object-filter.pipe';

describe('ArrayObjectFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayObjectFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
